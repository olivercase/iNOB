# Generated artifacts (regenerated by the pipeline)
outputs/
*.mat
*.npz
*.npy

# Visualisation outputs (regenerated)
*.png

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# Virtual environments
.venv/
venv/
env/

# Editor / OS
.idea/
.vscode/
.DS_Store
._*

# TetGen scratch artifacts
tetgen-tmpfile_*

# Cluster logs
cluster/logs/

# Node / frontend dependencies (regenerated by npm install)
node_modules/

# Raw BodyParts3D download archives (reproducible via scripts/download_bodyparts3d_4.3.py;
# the meshes themselves (.obj/.stl) are tracked via Git LFS)
*.zip
data/bodyparts3d/raw_4.3/_quarantine_*/

# Old git history moved aside during the iNOB migration
.git.old-*/

# GUI working config (generated)
configs/gui_working.yaml

# Raw BodyParts3D atlas + intermediates (regenerable; not needed by the pipeline)
data/bodyparts3d/
internal_meshes/

# Research-poster working folder (figures + analysis; not version-controlled)
poster_vagus_opmeg/
