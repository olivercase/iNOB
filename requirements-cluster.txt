# Cluster runtime constraints (UCL Myriad / Kathleen - manylinux2014).
# Older glibc forces a numpy<2.0 wheel and matching scipy/h5py majors.
# Used by cluster/build_duneuro.sh inside the Scratch venv.
numpy>=1.26,<2.0
scipy>=1.11,<1.14
h5py>=3.10,<3.12
trimesh>=4.0,<5.0
scikit-image>=0.22,<0.25
matplotlib>=3.8,<3.10
pyyaml>=6.0,<7.0
